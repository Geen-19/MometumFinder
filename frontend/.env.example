# Frontend Configuration
# Copy this to .env and adjust values as needed

# API base URL - leave empty for same-origin (Vercel deployment)
# For local dev with separate backend: VITE_API_BASE_URL=http://localhost:5000
VITE_API_BASE_URL=
